写代码之前先把思路想好, 前面想得越仔细, 代码写得简单些

- 定宽布局 ——把宽度用px写死, 不用百分比. 如果要适配多屏幕, 再写几个模版即可
- 通用部分的抽离
- 使用font-icon
- 抽离通用样式



```javascript
webpack.config.js

resolve : {
    // 给文件路径配置别名, 避免写 ../../../util/mm.js这样的代码, 容易出错
    alias : {
        node_modules    : __dirname + '/node_modules',
        util            : __dirname + '/src/util',
        page            : __dirname + '/src/page',
        service         : __dirname + '/src/service',
        image           : __dirname + '/src/image'
    }
  },
```

```javascript
/src/util/mm.js

var _mm = {
  // 网络请求
  request : function(param){
      var _this = this;
      $.ajax({
          type        : param.method  || 'get',
          url         : param.url     || '',
          dataType    : param.type    || 'json',
          data        : param.data    || '',
          success     : function(res){
              // 请求成功
              if(0 === res.status){
                  typeof param.success === 'function' && param.success(res.data, res.msg);
              }
              // 没有登录状态，需要强制登录
              else if(10 === res.status){
                  _this.doLogin();
              }
              // 请求数据错误
              else if(1 === res.status){
                  typeof param.error === 'function' && param.error(res.msg);
              }
          },
          error       : function(err){
              typeof param.error === 'function' && param.error(err.statusText);
          }
      });
  }
};

module.exports = _mm;
```

```html
/Users/sherrywong/Documents/GitHub/20190917imooc/doc/mmall-fe/src/page/index/index.js

require('./index.css');
var mm = require('util/mm.js');
```



安装依赖, font-awesome 和 hogan.js 都是上线项目要用的, 所以用 --save
```
npm install font-awesome hogan.js@3.0.2 --save
```

